"""З радістю! Ось компактна таблиця з базовим синтаксисом регулярних виразів у Python:

| Вираз       | Значення                              | Приклад                        |
|-------------|----------------------------------------|--------------------------------|
| `.`         | Будь-який символ (крім `\n`)           | `a.c` → знайде `abc`, `a-c`    |
| `^`         | Початок рядка                          | `^Hi` → знайде `Hi there`      |
| `$`         | Кінець рядка                           | `end$` → знайде `The end`      |
| `*`         | 0 або більше повторень                 | `bo*` → `b`, `bo`, `boo`       |
| `+`         | 1 або більше повторень                 | `go+` → `go`, `goo`            |
| `?`         | 0 або 1 повторення                     | `colou?r` → `color`/`colour`   |
| `[]`        | Один з набору символів                 | `[aeiou]` → голосна            |
| `|`         | АБО                                    | `cat|dog` → `cat` чи `dog`     |
| `\d`        | Будь-яка цифра                         | `\d+` → `2025`, `5`            |
| `\w`        | Літера, цифра або `_`                  | `\w+` → `word_1`               |
| `\s`        | Будь-який пробіл                       | `\s` → знаходить ` `, `\t`     |
| `()`        | Групування та збереження               | `(\d+)-(\d+)` → `12-47`        |

Хочеш, додам ще приклади з повною перевіркою `re.match` чи `re.findall`? Або можемо піти глибше — покажи, над чим працюєш!"""


"""
Вираз	Опис	Приклад коду	Результат
.	Будь-який символ	re.search(r'a.c', 'abc')	✅ match
^	Початок рядка	re.match(r'^Hi', 'Hi there')	✅ match
$	Кінець рядка	re.search(r'end$', 'That is the end')	✅ match
*	0 або більше повторень	re.search(r'ab*c', 'ac')	✅ match
+	1 або більше повторень	re.search(r'ab+c', 'abbc')	✅ match
?	0 або 1 повторення	re.search(r'colou?r', 'color')	✅ match
[]	Один з символів	re.search(r'[aeiou]', 'sky')	✅ match ('y')
`	`	АБО	`re.search(r'cat	dog', 'my dog')`	✅ match
\d	Будь-яка цифра	re.search(r'\d+', 'page 42')	✅ '42'
\w	Літера/цифра/підкреслення	re.search(r'\w+', 'hello!')	✅ 'hello'
\s	Пробіл	re.search(r'\s', 'hello world')	✅ match
()	Групування	re.search(r'(\d+)-(\d+)', '12-47')	✅ group(1): '12'
"""

